name: Just echo
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo 'Hello world'
      - name: print 
        run: echo ${{ github.event.workflow_dispatch.inputs.name }}
